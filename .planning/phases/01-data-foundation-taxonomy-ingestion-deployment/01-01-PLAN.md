---
phase: 01-data-foundation-taxonomy-ingestion-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/db.py
  - backend/app/models.py
  - backend/app/api/__init__.py
  - backend/app/api/companies.py
  - backend/app/ingest/__init__.py
  - backend/app/ingest/taxonomy.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/0001_initial_schema.py
  - backend/sectors.yaml
  - backend/tests/conftest.py
  - backend/tests/test_taxonomy.py
  - backend/tests/test_models.py
  - backend/tests/test_companies_endpoint.py
  - backend/Dockerfile
  - frontend/package.json
  - frontend/app/page.tsx
  - frontend/app/layout.tsx
  - frontend/Dockerfile
  - docker-compose.yml
  - .env.example
  - .gitignore
autonomous: true
requirements: [TAXO-01, STORE-01, DEPLOY-01]
must_haves:
  truths:
    - "Owner can edit backend/sectors.yaml (add/remove/retag a ticker) and the next taxonomy sync reflects the change with zero code edits"
    - "GET /companies returns one JSON object per ticker in sectors.yaml, each with a nested taxonomy block (ticker, name, sub_sector), a price field, and a fundamentals array"
    - "docker compose up --build starts backend and frontend together; the status page renders the live company count fetched server-side from the backend"
    - "A malformed sectors.yaml (missing required field, unknown field, or invalid YAML) fails the load with an explicit pydantic validation error naming the offending ticker/field, not a bare KeyError deep in ingestion"
    - "Re-running the taxonomy sync twice against an unchanged sectors.yaml leaves the tickers table row count unchanged (idempotent insert-or-ignore)"
    - "alembic upgrade head succeeds against a fresh SQLite file and env.py is configured with render_as_batch so later ALTER-TABLE migrations work on SQLite"
    - "DATABASE_URL is read from the environment via pydantic-settings with a SQLite default; no database connection string is hardcoded in application modules"
    - "Status page with 0 companies renders the 'No companies tracked yet' empty-state copy plus next-step guidance (UI-SPEC Copywriting Contract)"
    - "Status page renders the healthy state as the pluralized count line ('1 company tracked' singular vs '56 companies tracked' plural) with the green accent indicator"
    - "Status page renders the 'API unreachable' error copy plus refresh-to-retry instruction when the backend fetch returns non-200, fails, or exceeds its timeout"
    - "The server-side fetch from the status page to the backend sets an explicit 5s timeout so a hung backend falls through to the error state rather than hanging the page render"
    - "Status page has no client-visible loading spinner by design — it is a Server Component whose fetch resolves before the response is sent"
    - "Company count and page title cannot overflow their containers — count is a bounded small integer and the title is a fixed short string, so no truncation handling exists"
    - "All status page copy is fixed and pre-written — no user-generated or variable-length text renders on this page"
    - statement: "Concurrent SQLite access (nightly refresh writing while the API reads) does not raise 'database is locked' at this project's single-writer/once-daily volume"
      verification: backstop
  artifacts:
    - backend/sectors.yaml
    - backend/app/ingest/taxonomy.py
    - backend/app/models.py
    - backend/app/api/companies.py
    - backend/alembic/env.py
    - frontend/app/page.tsx
    - docker-compose.yml
  key_links:
    - "sectors.yaml -> TaxonomyConfig pydantic model -> tickers table (the TAXO-01 no-code-edit path)"
    - "tickers table -> GET /companies response -> frontend Server Component fetch -> rendered count (the full-stack proof, D-12)"
    - "pydantic-settings DATABASE_URL -> SQLAlchemy engine (the SQLite/Postgres swap point, STORE-01)"
---

<objective>
Land the Walking Skeleton: the thinnest end-to-end slice that proves taxonomy config, database, API, frontend, and container orchestration all work together. After this plan, the owner can run `docker compose up --build` and see a live company count on a web page that was read out of SQLite, which was populated from an owner-editable YAML file.

Purpose: Every later plan in this phase (prices, fundamentals, deploy) extends this skeleton rather than introducing new architecture. Getting the point-in-time schema and Alembic batch mode right here avoids the data migration that RESEARCH.md flags as the single most expensive retrofit in the system.

Output: Working backend + frontend + compose stack, `sectors.yaml` with all 56 tickers, all five SQLAlchemy models, first Alembic migration, and the `GET /companies` endpoint returning taxonomy-only payloads (price/fundamentals fill in during plans 02-04).
</objective>

## Phase Goal

**As a** solo investor tracking the data center value chain, **I want to** have every ticker's prices and SEC EDGAR fundamentals ingested with full provenance into a deployed pipeline whose taxonomy I can edit in YAML, **so that** I have a trustworthy data foundation to build every downstream screen and comparison on.

> **Planner note (flagged for confirmation):** ROADMAP.md's `**Goal:**` line for Phase 1 is stated in outcome prose ("Owner has a live, deployed data pipeline where...") rather than the `As a / I want to / so that` user-story form that MVP mode expects. The story above is a structural restatement of that exact goal — no new scope was invented. If you want the canonical story authored properly, run `/gsd mvp-phase 1` and re-plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-CONTEXT.md
@.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-RESEARCH.md
@.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-UI-SPEC.md
@.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-VALIDATION.md
@.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/SKELETON.md
@data-center-value-chain-tickers.md
</context>

## Artifacts this phase produces (plan 01)

**New files:** `backend/pyproject.toml`, `backend/sectors.yaml`, `backend/Dockerfile`, `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/versions/0001_initial_schema.py`, `frontend/Dockerfile`, `docker-compose.yml`, `.env.example`, `.gitignore`

**Python modules:** `app.main`, `app.config`, `app.db`, `app.models`, `app.api.companies`, `app.ingest.taxonomy`

**Classes:** `Settings` (pydantic-settings), `Base` (SQLAlchemy DeclarativeBase), `Ticker`, `Price`, `Fundamental`, `TickerCik`, `RefreshLog` (SQLAlchemy models), `TaxonomyEntry`, `TaxonomyConfig` (pydantic), `TaxonomyInfo`, `PriceSnapshot`, `FundamentalPeriod`, `CompanyResponse` (pydantic response schemas)

**Functions:** `get_settings()`, `get_engine()`, `get_session()`, `get_db()`, `load_taxonomy(path)`, `sync_taxonomy(session, config)`, `list_companies(db)`, `health()`

**Endpoints:** `GET /health`, `GET /companies`

**Frontend symbols:** `RootLayout` (`frontend/app/layout.tsx`), `StatusPage` default export (`frontend/app/page.tsx`)

**Env vars:** `DATABASE_URL`, `EDGAR_USER_AGENT`, `BACKEND_URL`

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — backend scaffold, test harness, and the failing end-to-end test</name>
  <files>backend/pyproject.toml, backend/app/__init__.py, backend/tests/conftest.py, backend/tests/test_companies_endpoint.py, backend/tests/fixtures/nvda_companyfacts.json, backend/tests/fixtures/tsm_companyfacts.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-VALIDATION.md (Wave 0 Requirements list — this task discharges the framework-install and conftest items)
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-RESEARCH.md (Validation Architecture section; Standard Stack install command; Pattern 2 for the fixture shapes you must record)
    - data-center-value-chain-tickers.md (the 56-ticker universe the end-to-end test asserts against)
  </read_first>
  <action>
Install `uv` first if absent (`pip install uv`) — RESEARCH.md Environment Availability confirms it is not on the local PATH, and CLAUDE.md mandates it over pip/venv/poetry.

Initialize the backend package with `uv init` under `backend/`, targeting Python 3.13 (`requires-python = ">=3.12,<3.14"` in `pyproject.toml`). Add runtime dependencies exactly as RESEARCH.md's verified install line specifies: `uv add fastapi "sqlalchemy>=2.0,<2.1" alembic pydantic-settings pyyaml yfinance tenacity httpx uvicorn`. Add dev dependencies: `uv add --dev ruff pytest pytest-mock respx`. Do not pin SQLAlchemy 2.1 — CLAUDE.md and RESEARCH.md both lock the 2.0 line.

Add a `[tool.pytest.ini_options]` block to `backend/pyproject.toml` with `testpaths = ["tests"]` and `pythonpath = ["."]` so `uv run pytest` resolves the `app` package without an editable install.

Add a `[tool.ruff]` block with `line-length = 100` and `target-version = "py312"`.

Create `backend/tests/conftest.py` exposing three pytest fixtures:
- `db_session` — creates a SQLAlchemy engine against an in-memory or `tmp_path`-backed SQLite URL, calls `Base.metadata.create_all`, yields a `Session`, and disposes on teardown.
- `nvda_facts` — loads `tests/fixtures/nvda_companyfacts.json` and returns the parsed dict.
- `tsm_facts` — loads `tests/fixtures/tsm_companyfacts.json` and returns the parsed dict.

Record the two fixture JSON files by fetching live EDGAR responses with an explicit `User-Agent` header (a generic UA returns 403 per RESEARCH.md Pitfall 3):
`curl -H "User-Agent: DataCenterStocks sureshkrip@gmail.com" https://data.sec.gov/api/xbrl/companyfacts/CIK0001045810.json` (NVDA, `us-gaap`) and `.../CIK0001046179.json` (TSM, `ifrs-full`). These are large; trim each recorded fixture down to only the concepts the pipeline reads — for NVDA keep `us-gaap.RevenueFromContractWithCustomerExcludingAssessedTax`, `us-gaap.NetIncomeLoss`, `dei.EntityCommonStockSharesOutstanding`; for TSM keep `ifrs-full.Revenue`, `ifrs-full.ProfitLossAttributableToOwnersOfParent`, `dei.EntityCommonStockSharesOutstanding`. Preserve the full nested `units` structure including TSM's `TWD` unit key alongside `USD` — the TWD entries are load-bearing for the plan 03 unit-filtering test and must not be stripped.

Write `backend/tests/test_companies_endpoint.py::test_companies_returns_full_taxonomy` as the phase's failing end-to-end test: it loads `backend/sectors.yaml`, syncs it to the test database, calls `GET /companies` through FastAPI's `TestClient`, and asserts the response is a 56-element list where every element has non-empty `taxonomy.ticker`, `taxonomy.name`, and `taxonomy.sub_sector`. This test MUST fail at the end of this task (the modules it imports do not exist yet) — that is the RED state Task 2 turns green.

Create a root `.gitignore` covering `.venv/`, `__pycache__/`, `*.pyc`, `.env`, `data/`, `*.db`, `node_modules/`, `.next/`. The `.env` and `data/` entries are security-relevant: RESEARCH.md Security Domain V1 requires secrets to live in env vars only and never be committed.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_companies_endpoint.py -x 2>&1 | grep -qE 'ModuleNotFoundError|ImportError|failed|error' && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` exists and `grep -c 'sqlalchemy' backend/pyproject.toml` is >= 1
    - `uv run ruff --version` succeeds from `backend/`
    - `backend/tests/conftest.py` defines fixtures named `db_session`, `nvda_facts`, `tsm_facts` — `grep -c 'def db_session' backend/tests/conftest.py` == 1
    - `python -c "import json;d=json.load(open('backend/tests/fixtures/nvda_companyfacts.json'));assert 'us-gaap' in d['facts'] and 'ifrs-full' not in d['facts']"` exits 0
    - `python -c "import json;d=json.load(open('backend/tests/fixtures/tsm_companyfacts.json'));assert 'ifrs-full' in d['facts'] and 'us-gaap' not in d['facts']"` exits 0
    - TSM fixture retains both currency unit keys: `python -c "import json;d=json.load(open('backend/tests/fixtures/tsm_companyfacts.json'));u=d['facts']['ifrs-full']['Revenue']['units'];assert 'USD' in u and 'TWD' in u"` exits 0
    - `test_companies_returns_full_taxonomy` exists and currently FAILS (red state confirmed)
    - `grep -c '^\.env$' .gitignore` == 1
  </acceptance_criteria>
  <done>Test harness installed, EDGAR fixtures recorded for both taxonomies, and the end-to-end taxonomy test exists and fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Taxonomy YAML to database to GET /companies — make the end-to-end test pass</name>
  <files>backend/sectors.yaml, backend/app/config.py, backend/app/db.py, backend/app/models.py, backend/app/ingest/taxonomy.py, backend/app/api/companies.py, backend/app/main.py, backend/alembic.ini, backend/alembic/env.py, backend/alembic/versions/0001_initial_schema.py, backend/tests/test_taxonomy.py, backend/tests/test_models.py</files>
  <read_first>
    - data-center-value-chain-tickers.md (all 56 tickers across 10 sub-sectors — the exact source for sectors.yaml, verified current per D-01)
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-RESEARCH.md (Pattern 4 point-in-time schema — copy the Fundamental model's column set and unique constraint verbatim; Pattern 6 Alembic batch mode; the FastAPI GET /companies response-model example)
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-CONTEXT.md (D-03 flat sub-sectors, D-04 watchlist as regular sub-sector, D-05 single file, D-06 minimal fields, D-08 nested response shape)
    - backend/tests/test_companies_endpoint.py (the failing test this task must turn green)
  </read_first>
  <behavior>
    - `load_taxonomy` on the real sectors.yaml returns a TaxonomyConfig with exactly 56 entries spanning 10 distinct sub_sector values
    - `load_taxonomy` on YAML missing a required per-ticker field raises pydantic ValidationError naming the field
    - `load_taxonomy` on YAML carrying an unrecognized per-ticker key raises pydantic ValidationError (extra fields forbidden)
    - `sync_taxonomy` run twice against the same config leaves the tickers row count at 56 (idempotent)
    - `sync_taxonomy` picks up a ticker whose sub_sector changed in YAML and updates the stored row without deleting others
    - A Fundamental row inserted twice with the same (ticker, fiscal_year, fiscal_period, accession_number) violates the unique constraint; the same tuple differing only in accession_number inserts as a second row
    - `GET /companies` returns 56 items, each with taxonomy populated, `price` null, and `fundamentals` an empty list
  </behavior>
  <action>
Author `backend/sectors.yaml` from `data-center-value-chain-tickers.md`, transcribing all 56 tickers. Per D-03 the structure is flat — one primary `sub_sector` string per ticker, no nesting. Per D-06 the only per-ticker fields are `ticker`, `name`, `sub_sector`; do not add a CIK field (CIK is pipeline-resolved and cached in plan 03) and do not add notes/alias fields. Per D-04 the emerging watchlist is a plain 10th sub-sector value with no special flag. Per D-05 this is one file, not split. Use these ten exact `sub_sector` values: `ai_chips`, `semi_equipment`, `memory_storage`, `networking`, `power_electrical`, `cooling_thermal`, `dc_reits`, `hyperscalers`, `construction_materials`, `emerging_watchlist`. VRT is assigned the single primary value `power_electrical` (D-03 permits no dual membership). CBRS (Cerebras, IPO 2026-05-14) belongs under `ai_chips` per D-01.

Write `backend/app/config.py` with a pydantic-settings `Settings` class exposing `database_url: str = "sqlite:////data/app.db"`, `edgar_user_agent: str`, and a `model_config` reading from a `.env` file. Expose a `get_settings()` accessor cached with `functools.lru_cache`. Every module reads configuration through this — no scattered `os.environ` access anywhere in the codebase (RESEARCH.md Don't Hand-Roll).

Write `backend/app/db.py` with `get_engine()` building a SQLAlchemy engine from `get_settings().database_url`, a `SessionLocal` factory, and a `get_db()` FastAPI dependency that yields a session and closes it in a finally block. Use only database-agnostic constructs so the Postgres swap needs no code change (STORE-01) — no SQLite-specific pragmas in the model layer and no raw SQL with SQLite-only syntax.

Write `backend/app/models.py` defining a `Base(DeclarativeBase)` and five SQLAlchemy 2.0 typed models using the `Mapped[]` / `mapped_column` idiom exclusively — never the 1.x `Column`/`Query` style:
- `Ticker` — `ticker` (String(10), primary key), `name` (String), `sub_sector` (String, indexed)
- `Price` — `id` pk, `ticker` (String(10), indexed), `close` (Numeric), `as_of` (Date), `source` (String, default `"yfinance"`), plus a UniqueConstraint on (`ticker`, `as_of`) named `uq_price_ticker_date`
- `Fundamental` — reproduce RESEARCH.md Pattern 4's column set exactly: `id`, `ticker`, `fiscal_year`, `fiscal_period`, `form`, `accession_number`, `filed_date`, `period_end`, `revenue`, `net_income`, `market_cap`, `source` (default `"SEC EDGAR"`), `taxonomy`; with UniqueConstraint(`ticker`, `fiscal_year`, `fiscal_period`, `accession_number`) named `uq_fundamental_period_filing`. This unique key is the point-in-time guarantee behind D-09 — a key on ticker alone would silently discard filing history on every re-run.
- `TickerCik` — `ticker` (String(10), primary key), `cik` (String(10)), `resolved_at` (DateTime)
- `RefreshLog` — `id` pk, `run_id` (String), `started_at`/`finished_at` (DateTime), `tickers_attempted` (Integer), `failure_count` (Integer), `failures` (JSON or Text)

Configure Alembic: run `uv run alembic init alembic` inside `backend/`, then edit `alembic/env.py` to import `Base.metadata` as `target_metadata`, read the URL from `get_settings().database_url`, and pass `render_as_batch=True` to `context.configure` in both the online and offline paths. RESEARCH.md Pitfall 6 is explicit that this must be set from the FIRST migration, not retroactively — otherwise the second migration fails on SQLite while passing on Postgres. Generate `0001_initial_schema.py` via `uv run alembic revision --autogenerate -m "initial schema"` and confirm it creates all five tables plus both named unique constraints.

Write `backend/app/ingest/taxonomy.py` with a pydantic `TaxonomyEntry` model (`ticker`, `name`, `sub_sector`, all required) configured with `extra="forbid"` so a typo'd key in the owner-edited YAML fails loudly rather than being silently ignored — TAXO-01's entire premise is owner-editable config, so validation errors must be clear and immediate. Add a `TaxonomyConfig` wrapper holding `companies: list[TaxonomyEntry]`. `load_taxonomy(path)` reads the file and parses it with `yaml.safe_load` — the unsafe full-object loader must never be used here, since it permits arbitrary Python object deserialization from a file that anyone with repo write access can edit (RESEARCH.md Security Domain V5, threat T-01-01). `sync_taxonomy(session, config)` upserts each entry into the `tickers` table keyed on `ticker`, updating `name`/`sub_sector` when they changed and inserting when absent. Per D-02a it MUST NOT delete rows for tickers absent from the YAML — delisted tickers stay in the taxonomy permanently and are removed only by a deliberate owner edit, never by the pipeline.

Write `backend/app/api/companies.py` with the pydantic response models from RESEARCH.md's D-08 example — `TaxonomyInfo`, `PriceSnapshot`, `FundamentalPeriod`, `CompanyResponse` — and a `list_companies(db)` route registered as `GET /companies` with `response_model=list[CompanyResponse]`. In this plan it returns every `Ticker` row with `price=None` and `fundamentals=[]`; plans 02 and 04 populate those fields against this same unchanged response contract. Keep these Pydantic schemas separate from the SQLAlchemy models — RESEARCH.md's Alternatives Considered explains that merging them (SQLModel) would force provenance columns into every response shape.

Define the route as a plain synchronous function, never a coroutine — CLAUDE.md and RESEARCH.md both lock sync FastAPI routes plus sync SQLAlchemy for this project; FastAPI runs sync routes in a threadpool automatically, so this costs nothing in concurrency.

Per D-07 this endpoint is the phase's proof surface for success criterion #2 — a real, permanent part of the API that Phase 2's frontend will consume, not a throwaway CLI report. Build it to that standard.

Write `backend/app/main.py` creating the FastAPI app, including the companies router, and adding a `GET /health` route returning `{"status": "ok"}`. Register a global exception handler that logs the exception server-side and returns a generic JSON error body — stack traces must never reach API clients (RESEARCH.md Security Domain V7, threat T-01-04).

Write `backend/tests/test_taxonomy.py` and `backend/tests/test_models.py` covering every bullet in the behavior block above, including `test_load_sectors_yaml` and `test_crud_roundtrip` by those exact names (01-VALIDATION.md's per-task verification map references them).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_taxonomy.py tests/test_models.py tests/test_companies_endpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml;d=yaml.safe_load(open('backend/sectors.yaml'));c=d['companies'];assert len(c)==56, len(c);assert len({e['sub_sector'] for e in c})==10"` exits 0
    - CBRS present under ai_chips: `python -c "import yaml;c=yaml.safe_load(open('backend/sectors.yaml'))['companies'];assert any(e['ticker']=='CBRS' and e['sub_sector']=='ai_chips' for e in c)"` exits 0
    - No identifier field beyond the three declared keys leaked into the taxonomy (D-06): `python -c "import yaml;c=yaml.safe_load(open('backend/sectors.yaml'))['companies'];assert all(set(e)=={'ticker','name','sub_sector'} for e in c)"` exits 0
    - Safe YAML loader only — unsafe full-object loading is absent from the taxonomy module: `grep -v '^#' backend/app/ingest/taxonomy.py | grep -c 'yaml\.load(' ` == 0
    - Batch mode configured: `grep -c 'render_as_batch=True' backend/alembic/env.py` >= 1
    - Point-in-time unique constraint present: `grep -c 'uq_fundamental_period_filing' backend/app/models.py` == 1
    - Migration applies cleanly to a fresh database: `cd backend && rm -f /tmp/t.db && DATABASE_URL=sqlite:////tmp/t.db uv run alembic upgrade head` exits 0
    - All five tables created: `python -c "import sqlite3;n={r[0] for r in sqlite3.connect('/tmp/t.db').execute('select name from sqlite_master where type=\"table\"')};assert {'tickers','prices','fundamentals','ticker_ciks','refresh_logs'} <= n, n"` exits 0
    - `test_companies_returns_full_taxonomy` now PASSES (green)
    - `grep -c 'async def' backend/app/api/companies.py` == 0 (sync routes per locked stack decision)
  </acceptance_criteria>
  <done>sectors.yaml holds all 56 tickers, the schema migrates cleanly on SQLite with batch mode, and GET /companies returns all 56 taxonomy payloads — the Wave 0 end-to-end test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Next.js status page and docker-compose — the full stack runs as one command</name>
  <files>frontend/package.json, frontend/app/layout.tsx, frontend/app/page.tsx, frontend/Dockerfile, backend/Dockerfile, docker-compose.yml, .env.example</files>
  <read_first>
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-UI-SPEC.md (the full visual contract — spacing scale, typography roles, the four color values, and every copy string; this task must match it exactly)
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-RESEARCH.md (docker-compose skeleton under Code Examples; Coolify volume/env-var conventions)
    - .planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-CONTEXT.md (D-12 — minimal status page scope, explicitly not the Phase 2 table UI)
    - backend/app/main.py (the /health and /companies contract the frontend consumes)
  </read_first>
  <action>
Scaffold the frontend with `npx create-next-app@latest frontend --typescript --app --no-src-dir --eslint`, targeting Next.js 16.x. Node 20.9+ is required and the local machine has v22.16.0. Do not add shadcn or any component library — 01-UI-SPEC.md's Design System section explicitly defers the shadcn init gate to Phase 2, because Phase 1's entire UI surface is one text readout with no forms, tables, or interactive controls.

Do not wire `next lint` into the build — it was removed entirely in Next.js 16 and `next build` no longer runs linting as a side effect. Use the ESLint config `create-next-app` generates, invoked directly.

Write `frontend/app/page.tsx` as an async Server Component — it must carry no client-boundary directive pragma at the top of the file. It fetches `${process.env.BACKEND_URL}/companies` server-side with `cache: "no-store"` and an explicit 5-second timeout via `AbortSignal.timeout(5000)`. 01-UI-SPEC.md's `loading` row requires this timeout specifically so a hung backend falls through to the error state instead of hanging the page render indefinitely. If any dynamic route API is touched, it must be awaited — Next.js 16 made `params`/`searchParams`/`cookies()`/`headers()` async-only, and sync access patterns from older tutorials silently misbehave.

Render exactly three states, using the copy strings from 01-UI-SPEC.md's Copywriting Contract verbatim:
- Fetch failed, timed out, or non-200 → the error state: heading "API unreachable", body "Could not reach the backend. Confirm the backend service is running, then refresh this page." Use the destructive color `#DC2626` for the status dot and status text.
- 200 with an empty array → the empty state: heading "No companies tracked yet", body "sectors.yaml has no tickers, or ingestion hasn't run yet. Check the taxonomy config and run the refresh script."
- 200 with N > 0 → the healthy state: the count rendered at Display size (32px/600), the line "{count} companies tracked" pluralized so N==1 renders "1 company tracked", and the secondary line "API: healthy · as of {timestamp}" at Label size (14px/400). Use the accent color `#16A34A` for the dot and status text.

Apply 01-UI-SPEC.md's tokens exactly: page background `#FFFFFF`, status card surface `#F4F5F7`, card internal padding 24px, page title to card gap 32px, status line to timestamp gap 8px, page top/bottom margin 64px. Page title is "Data Center Stocks" at Heading size (24px/600). Only the four declared type sizes (32/24/16/14) and two weights (600/400) may appear. The accent green is reserved for the healthy indicator alone and the destructive red for the error indicator alone — nothing else on this page uses either color.

Write `backend/Dockerfile` on a `python:3.13-slim` base, installing `uv`, copying `pyproject.toml`/`uv.lock` and running `uv sync --frozen` before copying app source (layer-cache ordering), then `CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`. Write `frontend/Dockerfile` as a multi-stage Node 22 build running `next build` then serving with `next start` on port 3000.

Write `docker-compose.yml` with exactly two services and no `db` service — RESEARCH.md's recommendation is a Coolify-mounted SQLite volume for the MVP, since adding a Postgres container before it is needed is the premature complexity the SQLite-first decision exists to avoid. Do not declare a `networks:` block; Coolify auto-creates a shared network per compose stack. Configure:
- `backend`: builds `./backend`, environment `DATABASE_URL=${DATABASE_URL:-sqlite:////data/app.db}` and `EDGAR_USER_AGENT=${EDGAR_USER_AGENT}`, with a bind volume mounting `./data` at `/data`. No published port — it is reached internally at `http://backend:8000`.
- `frontend`: builds `./frontend`, environment `BACKEND_URL=http://backend:8000`, port 3000 published for local runs (Coolify assigns the external domain in plan 05).

Every value in the compose file must come through `${VAR}` substitution — no literal connection strings or credentials may appear in this committed file (RESEARCH.md Security Domain, threat T-01-03). Write `.env.example` documenting `DATABASE_URL` and `EDGAR_USER_AGENT` with placeholder values and a comment that `EDGAR_USER_AGENT` must be a descriptive app name plus contact email, because SEC EDGAR returns 403 and may IP-block for ~10 minutes on requests carrying a generic User-Agent — that failure mode takes down every remaining ticker in a run, not just one.

Have the backend container run `alembic upgrade head` on startup before launching uvicorn so a fresh volume self-migrates.
  </action>
  <verify>
    <automated>docker compose up --build -d && sleep 25 && curl -sf http://localhost:3000 | grep -qi 'Data Center Stocks' && curl -sf http://localhost:8000/health || (docker compose logs --tail=50; false)</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 and declares exactly two services: `docker compose config --services | wc -l` == 2
    - No database container was introduced: `docker compose config --services | grep -c '^db$'` == 0
    - No hardcoded secrets in the committed compose file — the DATABASE_URL line uses variable substitution: `grep -c 'DATABASE_URL=\${' docker-compose.yml` >= 1
    - `curl -sf http://localhost:8000/health` returns HTTP 200 with a JSON body containing `ok`
    - `curl -sf http://localhost:8000/companies | python -c "import json,sys;assert len(json.load(sys.stdin))==56"` exits 0
    - Status page renders the populated state with the pluralized count: `curl -sf http://localhost:3000 | grep -q '56 companies tracked'`
    - Page title renders: `curl -sf http://localhost:3000 | grep -qi 'Data Center Stocks'`
    - Status page is a Server Component: `grep -c 'use client' frontend/app/page.tsx` == 0
    - Explicit fetch timeout present per UI-SPEC loading row: `grep -c 'AbortSignal.timeout' frontend/app/page.tsx` >= 1
    - Both UI-SPEC state copy strings present in source: `grep -c 'No companies tracked yet' frontend/app/page.tsx` >= 1 and `grep -c 'API unreachable' frontend/app/page.tsx` >= 1
    - Error state verified behaviorally: stop the backend (`docker compose stop backend`), reload the page, and confirm it renders "API unreachable" rather than a Next.js error overlay or a hanging request; then `docker compose start backend`
    - No component library was installed this phase: `test ! -f frontend/components.json`
  </acceptance_criteria>
  <done>`docker compose up --build` brings up backend and frontend together; the status page shows "56 companies tracked" read live from SQLite through the API, and degrades to the specified error copy when the backend is down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo file → ingestion process | `sectors.yaml` is a file-on-disk parsed into live Python objects at ingest time |
| public internet → frontend/backend HTTP | once deployed, the Coolify domain is reachable by anyone who learns the URL |
| container → host filesystem | the SQLite volume bind-mounts host storage into the backend container |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `app/ingest/taxonomy.py` YAML parse | high | mitigate | Parse with `yaml.safe_load` exclusively; the unsafe full-object loader permits arbitrary Python object construction from a config file. Enforced by the Task 2 acceptance-criteria grep gate. Pair with a pydantic schema using `extra="forbid"`. |
| T-01-02 | Information Disclosure | `GET /companies` on a public Coolify domain | medium | accept | REQUIREMENTS.md locks multi-user auth out of v1 scope ("single personal user"), so no application-level auth is built. The exposed data is public-company pricing and SEC-filed fundamentals — already public information — so disclosure impact is low. Accepted risk; the network-level mitigation options are surfaced to the owner at the plan 05 deployment checkpoint. |
| T-01-03 | Information Disclosure | committed `docker-compose.yml` / `.env.example` | high | mitigate | All environment values pass through `${VAR}` substitution sourced from Coolify's env-var UI or a gitignored `.env`; no literal connection strings or credentials in either committed file. `.env` and `data/` are in `.gitignore` from Task 1. |
| T-01-04 | Information Disclosure | FastAPI error responses | medium | mitigate | Global exception handler logs server-side and returns a generic JSON error body; stack traces and internal paths never reach API clients (ASVS V7). |
| T-01-05 | Denial of Service | frontend server-side fetch to backend | medium | mitigate | Explicit 5s `AbortSignal.timeout` on the status page fetch so an unresponsive backend renders the error state instead of stalling the Next.js render worker. |

ASVS level 1; blocking threshold `high`. Both `high`-severity threats (T-01-01, T-01-03) carry `mitigate` dispositions with acceptance-criteria gates attached.
</threat_model>

<verification>
- `cd backend && uv run pytest tests/ -q` — full suite green
- `docker compose up --build -d` then `curl -sf http://localhost:8000/companies` returns 56 entries and `curl -sf http://localhost:3000` renders "56 companies tracked"
- Edit `backend/sectors.yaml` to change one ticker's `sub_sector`, re-run the taxonomy sync, and confirm `GET /companies` reflects the change with no code edit (TAXO-01 proof)
- `DATABASE_URL=sqlite:////tmp/fresh.db uv run alembic upgrade head` succeeds against an empty file
</verification>

<success_criteria>
- All 56 tickers from `data-center-value-chain-tickers.md` are in `sectors.yaml` across the 10 declared sub-sectors, with no CIK or notes fields
- `GET /companies` returns the D-08 nested shape for every ticker
- The status page renders the live count and degrades to the UI-SPEC error copy when the backend is unreachable
- The five-model schema — including the `(ticker, fiscal_year, fiscal_period, accession_number)` unique key — migrates cleanly on SQLite with `render_as_batch=True`
- `docker compose up --build` is the single command that runs the whole stack
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-taxonomy-ingestion-deployment/01-01-SUMMARY.md` when done
</output>
